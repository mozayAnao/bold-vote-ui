import styled from "styled-components";

export const DashboardWrapper = styled.div`
  width: 100%;
  min-height: 91.5vh;

  & .main {
    position: relative;
    overflow: hidden;
    transition: all 0.15s;
    padding: 0 20px;
    margin-left: 64px;
  }
`;
